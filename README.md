Create an IP protocols database from socket.IPPROTO...
and host system protocols db (/etc/protocols).

Primarily suggested as a tool to ease the expansion of the
IP proto list in pox.lib.packet.packet_utils

